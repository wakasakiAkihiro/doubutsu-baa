# QA記録

検証日：2026年9月11日。対象：ローカルで配信した本番ビルド、およびVercelのProduction URL。

## 自動検証

| 項目           | 結果                        | 検証内容                                                                                                                     |
| -------------- | --------------------------- | ---------------------------------------------------------------------------------------------------------------------------- |
| Unit           | 15件成功                    | 30種の定義と実ファイル、シャッフル100周、周回境界、先読み、チャイムと音声の利用不可・再開失敗・消音・発音失敗                |
| UI             | 8件成功                     | 開始、登場、反応、次のターン、連打、7秒の自動進行と待ち時間リセット、消音、バックグラウンド停止、画像フォールバック          |
| Playwright E2E | 40件成功・8件は意図的に省略 | 8環境で主要操作・連打・音声失敗・動きの軽減・キーボード操作。音声同期は対応ブラウザで確認、全30種×2周はデスクトップで1回実施 |
| Typecheck      | 成功                        | TypeScriptの厳格な型検査                                                                                                     |
| Lint           | 成功                        | Oxlint                                                                                                                       |
| Format         | 成功                        | Prettier                                                                                                                     |
| Build          | 成功                        | TypeScript + Vite production build                                                                                           |

E2Eは再試行なしで成功しました。全種類の実画像が読み込まれ、一巡内の重複と周回境界での連続がないことを確認しています。

## ブラウザ・表示確認

| 環境                             | サイズ     | 結果 |
| -------------------------------- | ---------- | ---- |
| Chromium・スマートフォン相当     | 390 × 844  | 成功 |
| Chromium・スマートフォン相当     | 412 × 915  | 成功 |
| Chromium・タブレット相当         | 768 × 1024 | 成功 |
| Chromium・デスクトップ           | 1440 × 900 | 成功 |
| Chromium・高さの低いPC           | 1258 × 622 | 成功 |
| Chromium・小型スマートフォン相当 | 320 × 568  | 成功 |
| WebKit・iPhone相当               | 390 × 844  | 成功 |
| Chromium・横向き                 | 844 × 390  | 成功 |

開始画面、隠れている状態、登場後のスクリーンショットを保存し、代表サイズを目視確認しました。動物の切れ、横スクロール、見出しとの重なりを修正・検証済みです。次のボタンも画面内に収まります。主要操作中のHTTPエラー、console error、未処理例外はありませんでした。

画面中央の広い領域がタップ可能で、開始ボタンは高さ60 px以上、動物は小型画面でも220 px以上あります。連打で複数ターンを飛ばさず、音声初期化が失敗しても遊びを続けられます。

## 画像

30 / 30種を共通の絵本画風で個別生成しました。最終画像はすべて600 × 600 pxの透明WebPです。合計1,765,512 bytes、最大75,096 bytes。りすの初回画像に残っていた背景を画像生成で修正し、最終版の透明背景を確認しました。

元画像や一時ファイルはGitに含めず、アプリで使用する最終WebPのみを配信します。再生成用の指示は [image-prompts.json](image-prompts.json) に記録しています。

## 本番公開・実機確認

本番URL：[https://doubutsu-baa.vercel.app](https://doubutsu-baa.vercel.app)

- Vercelチーム：`akikojohi0178-5709s-projects`（指定されたチーム）
- 新規プロジェクト：`doubutsu-baa`
- Project ID：`prj_VOfeI1dZY4biZ8hw5A86VEAkZxhF`
- 公開状態：Production / READY
- 初回公開時のアプリコミット：`9659988`（ゲーム実装は `d146e73`）
- 本番HTTP応答：200。ログイン不要で利用可能。
- 本番E2E：音声追加後に再実行します。上記8環境と、全30種×2周の画像読み込み、開始・登場・反応・次のターン、連打、音声失敗時の継続、消音、動きの軽減、キーボード操作を確認対象とします。
- 主要操作中のHTTPエラー・console error・未処理例外：なし。
- 本番の画像とレイアウトをChromiumで撮影・目視確認済み。
- CSP、`X-Content-Type-Options: nosniff` の本番配信を確認済み。

初回のWebKit検証では、Playwrightのスクリーンショット処理が挿入する `body {}` の空スタイルがCSPに拒否されました。Playwrightの実装を確認して原因を特定し、画面撮影をChromiumで行うように修正しています。WebKitではCSPとconsole監視を有効なまま操作テストを再実行し、3件すべて成功しました。本番アプリのセキュリティ設定を緩める変更はしていません。

WebKit検証はブラウザエンジンによるエミュレーションです。物理的なiPhone・Androidでのタッチ感覚、端末の消音設定、効果音の聴感確認は未実施です。端末では開始・登場・次のターン、音量と消音、縦横表示を確認してください。

再検証方法は [README](../README.md) に記載しています。
